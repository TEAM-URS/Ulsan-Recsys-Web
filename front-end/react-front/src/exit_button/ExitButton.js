import styled from '@emotion/styled';
import Button from '@mui/material/Button';

// rounded 버튼 ExitButton 컴포넌트 생성
const ExitButton = styled(Button)`
border-radius: 50px;
`;

export default ExitButton;